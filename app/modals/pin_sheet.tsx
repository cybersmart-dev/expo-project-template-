import { View, Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const pin_sheet = () => {
  return (
    <SafeAreaView style={{}} className=' flex-1'>
      <Text>pin_sheet</Text>
    </SafeAreaView>
  )
}

export default pin_sheet